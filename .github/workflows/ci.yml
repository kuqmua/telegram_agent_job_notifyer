name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  CARGO_NET_TIMEOUT: 60
  RUSTFLAGS: "-D warnings -D unreachable_pub -D dead_code"
  RUSTDOCFLAGS: "-D warnings"
  RUST_BACKTRACE: short

jobs:
  changed-files:
    name: Detect changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      ci: ${{ steps.filter.outputs.ci }}
      architecture_code: ${{ steps.filter.outputs.architecture_code }}
      architecture_docs: ${{ steps.filter.outputs.architecture_docs }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            rust:
              - '**/*.rs'
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - 'rust-toolchain.toml'
              - '.cargo/config.toml'
            ci:
              - '.github/workflows/**'
            architecture_code:
              - 'server/src/**'
              - 'telegram_agent_shared/src/**'
            architecture_docs:
              - 'docs/architecture.md'
              - 'README.md'
              - 'server/README.md'

  fmt:
    name: Formatting
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly-2026-04-15
        with:
          components: rustfmt
      - run: cargo fmt --check

  clippy:
    name: Clippy
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly-2026-04-15
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  build:
    name: Build
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        profile: [dev, release]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly-2026-04-15
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          key: ${{ matrix.profile }}
      - name: Build (${{ matrix.profile }})
        run: cargo build --all-targets --all-features ${{ matrix.profile == 'release' && '--release' || '' }}
      - name: Upload artifacts (release)
        if: matrix.profile == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: release-binaries
          path: target/release/*server*
          if-no-files-found: warn
          retention-days: 7

  test:
    name: Test
    needs: [clippy]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly-2026-04-15
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Install nextest
        uses: taiki-e/install-action@cargo-nextest
      - name: Test
        run: cargo nextest run --all-targets --all-features --profile ci
        env:
          RUSTFLAGS: "-D warnings"
      - name: Doc tests
        run: cargo test --doc --all-features

  doc:
    name: Docs
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly-2026-04-15
      - uses: Swatinem/rust-cache@v2
      - name: Check docs
        run: cargo doc --all-features --no-deps --document-private-items

  msrv:
    name: MSRV
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.85.0
      - name: Check MSRV
        run: cargo check --all-features

  audit:
    name: Security audit
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  secret-scan:
    name: Secret scan
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docs-architecture-guard:
    name: Architecture docs guard
    needs: changed-files
    if: needs.changed-files.outputs.architecture_code == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Validate architecture docs changed
        run: |
          if [ "${{ needs.changed-files.outputs.architecture_docs }}" = "true" ]; then
            echo "Architecture documentation changed"
            exit 0
          fi
          echo "::error::Architecture code changed without docs update (docs/architecture.md or README)"
          exit 1

  machete:
    name: Unused deps
    needs: changed-files
    if: needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly-2026-04-15
      - name: Install cargo-machete
        uses: taiki-e/install-action@cargo-machete
      - run: cargo machete

  check-semver:
    name: Semver check
    needs: changed-files
    if: github.event_name == 'pull_request' && (needs.changed-files.outputs.rust == 'true' || needs.changed-files.outputs.ci == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@nightly-2026-04-15
      - uses: obi1kenobi/cargo-semver-checks-action@v2

  ci-success:
    name: CI passed
    if: always()
    runs-on: ubuntu-latest
    needs: [fmt, clippy, build, test, doc, msrv, audit, secret-scan, docs-architecture-guard, machete]
    permissions:
      contents: none
    steps:
      - name: Check all jobs passed
        run: |
          echo "fmt:      ${{ needs.fmt.result }}"
          echo "clippy:   ${{ needs.clippy.result }}"
          echo "build:    ${{ needs.build.result }}"
          echo "test:     ${{ needs.test.result }}"
          echo "doc:      ${{ needs.doc.result }}"
          echo "msrv:     ${{ needs.msrv.result }}"
          echo "audit:    ${{ needs.audit.result }}"
          echo "secret-scan: ${{ needs.secret-scan.result }}"
          echo "docs-architecture-guard: ${{ needs.docs-architecture-guard.result }}"
          echo "machete:  ${{ needs.machete.result }}"
          # skipped jobs (no changes) count as success
          for r in "${{ needs.fmt.result }}" "${{ needs.clippy.result }}" "${{ needs.build.result }}" "${{ needs.test.result }}" "${{ needs.doc.result }}" "${{ needs.msrv.result }}" "${{ needs.audit.result }}" "${{ needs.secret-scan.result }}" "${{ needs.docs-architecture-guard.result }}" "${{ needs.machete.result }}"; do
            if [ "$r" = "failure" ] || [ "$r" = "cancelled" ]; then
              echo "::error::Job with result '$r' failed"
              exit 1
            fi
          done
          echo "All checks passed"
